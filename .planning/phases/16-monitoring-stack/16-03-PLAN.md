---
phase: 16-monitoring-stack
plan: 03
type: execute
wave: 3
depends_on: ["16-01", "16-02"]
files_modified:
  - monitoring/grafana/provisioning/dashboards/dashboards.yaml
  - monitoring/grafana/provisioning/dashboards/alexandria-rag.json
  - monitoring/grafana/provisioning/alerting/alerts.yaml
autonomous: false

must_haves:
  truths:
    - "Grafana dashboard shows search latency panels (P50, P95, P99)"
    - "Grafana dashboard shows ingestion rate panel"
    - "Grafana dashboard shows error rate panel"
    - "Grafana dashboard shows logs panel from Loki"
    - "Alert fires when search latency P95 exceeds 2 seconds"
    - "Alert fires when error rate exceeds threshold"
  artifacts:
    - path: "monitoring/grafana/provisioning/dashboards/dashboards.yaml"
      provides: "Dashboard provider configuration"
      contains: "type: file"
    - path: "monitoring/grafana/provisioning/dashboards/alexandria-rag.json"
      provides: "RAG-specific Grafana dashboard"
      contains: "alexandria_search_duration"
    - path: "monitoring/grafana/provisioning/alerting/alerts.yaml"
      provides: "Alert rules for latency and errors"
      contains: "High Search Latency"
  key_links:
    - from: "alexandria-rag.json"
      to: "VictoriaMetrics"
      via: "datasource queries"
      pattern: "alexandria_search_duration_seconds"
    - from: "alexandria-rag.json"
      to: "Loki"
      via: "log panel queries"
      pattern: "datasource.*Loki"
    - from: "alerts.yaml"
      to: "VictoriaMetrics"
      via: "alert expressions"
      pattern: "histogram_quantile"
---

<objective>
Create Grafana dashboards and alerts for RAG-specific metrics

Purpose: Enable visual monitoring of search latency, ingestion rate, and error rates with automated alerting
Output: Preconfigured dashboard with latency, throughput, and log panels; alerts on latency and error thresholds
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-monitoring-stack/16-RESEARCH.md
@.planning/phases/16-monitoring-stack/16-01-SUMMARY.md
@.planning/phases/16-monitoring-stack/16-02-SUMMARY.md
@.planning/phases/15-metrics-foundation/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard provider and RAG dashboard</name>
  <files>
    - monitoring/grafana/provisioning/dashboards/dashboards.yaml
    - monitoring/grafana/provisioning/dashboards/alexandria-rag.json
  </files>
  <action>
1. Create `monitoring/grafana/provisioning/dashboards/dashboards.yaml`:
   ```yaml
   apiVersion: 1
   providers:
     - name: 'Alexandria'
       orgId: 1
       folder: 'Alexandria'
       type: file
       disableDeletion: true
       editable: false
       options:
         path: /etc/grafana/provisioning/dashboards
   ```

2. Create `monitoring/grafana/provisioning/dashboards/alexandria-rag.json` with these panels:

**Row 1: Overview (4 stat panels)**
- Search Rate: `rate(alexandria_search_duration_seconds_count[5m])`
- Ingestion Count (1h): `increase(alexandria_documents_ingested_total[1h])`
- Error Rate: `rate(http_server_requests_seconds_count{status=~"5.."}[5m])`
- Uptime: Use Grafana's built-in uptime calculation or `process_uptime_seconds`

**Row 2: Latency (3 time series panels)**
- Search Latency P50: `histogram_quantile(0.50, sum(rate(alexandria_search_duration_seconds_bucket[5m])) by (le))`
- Search Latency P95: `histogram_quantile(0.95, sum(rate(alexandria_search_duration_seconds_bucket[5m])) by (le))`
- Search Latency P99: `histogram_quantile(0.99, sum(rate(alexandria_search_duration_seconds_bucket[5m])) by (le))`

**Row 3: Embedding Latency (2 time series panels)**
- Embedding Latency P95: `histogram_quantile(0.95, sum(rate(alexandria_embedding_duration_seconds_bucket[5m])) by (le))`
- Embedding Latency Heatmap: `sum(rate(alexandria_embedding_duration_seconds_bucket[5m])) by (le)`

**Row 4: Throughput (2 time series panels)**
- Documents Ingested Over Time: `increase(alexandria_documents_ingested_total[5m])`
- Search Queries Over Time: `rate(alexandria_search_duration_seconds_count[5m])`

**Row 5: Logs (1 logs panel)**
- Loki datasource with query: `{service=~"app|alexandria-app"}`
- Show last 100 log lines

Dashboard settings:
- Title: "Alexandria RAG"
- UID: "alexandria-rag"
- Refresh: 30s
- Time range: Last 1 hour
- Datasource variable for VictoriaMetrics

Note: The JSON should be a valid Grafana dashboard export. Use datasource references by name (VictoriaMetrics, Loki).
  </action>
  <verify>
    # Verify dashboard JSON is valid
    python3 -c "import json; json.load(open('monitoring/grafana/provisioning/dashboards/alexandria-rag.json'))"

    # Check dashboard contains expected metrics
    grep -E "alexandria_search_duration|alexandria_documents_ingested|alexandria_embedding_duration" monitoring/grafana/provisioning/dashboards/alexandria-rag.json
  </verify>
  <done>Dashboard provider configured, RAG dashboard created with latency, throughput, and log panels</done>
</task>

<task type="auto">
  <name>Task 2: Create alert rules for latency and errors</name>
  <files>
    - monitoring/grafana/provisioning/alerting/alerts.yaml
  </files>
  <action>
1. Create `monitoring/grafana/provisioning/alerting/alerts.yaml`:
   ```yaml
   apiVersion: 1
   groups:
     - orgId: 1
       name: Alexandria Alerts
       folder: Alexandria
       interval: 60s
       rules:
         - uid: alexandria-search-latency
           title: High Search Latency
           condition: C
           data:
             - refId: A
               relativeTimeRange:
                 from: 300
                 to: 0
               datasourceUid: victoriametrics
               model:
                 expr: histogram_quantile(0.95, sum(rate(alexandria_search_duration_seconds_bucket[5m])) by (le))
                 refId: A
             - refId: B
               relativeTimeRange:
                 from: 300
                 to: 0
               datasourceUid: __expr__
               model:
                 type: reduce
                 expression: A
                 reducer: last
                 refId: B
             - refId: C
               datasourceUid: __expr__
               model:
                 type: threshold
                 expression: B
                 conditions:
                   - evaluator:
                       type: gt
                       params: [2]
                 refId: C
           for: 60s
           noDataState: NoData
           execErrState: Error
           annotations:
             summary: Search latency P95 exceeds 2 seconds
             description: "Current P95 latency: {{ $values.B.Value }}s"

         - uid: alexandria-error-rate
           title: High Error Rate
           condition: C
           data:
             - refId: A
               relativeTimeRange:
                 from: 300
                 to: 0
               datasourceUid: victoriametrics
               model:
                 expr: rate(http_server_requests_seconds_count{status=~"5.."}[5m])
                 refId: A
             - refId: B
               relativeTimeRange:
                 from: 300
                 to: 0
               datasourceUid: __expr__
               model:
                 type: reduce
                 expression: A
                 reducer: last
                 refId: B
             - refId: C
               datasourceUid: __expr__
               model:
                 type: threshold
                 expression: B
                 conditions:
                   - evaluator:
                       type: gt
                       params: [0.1]
                 refId: C
           for: 60s
           noDataState: NoData
           execErrState: Error
           annotations:
             summary: Error rate exceeds 0.1 req/s
             description: "Current error rate: {{ $values.B.Value }} req/s"
   ```

Note:
- datasourceUid must match the UID Grafana assigns (use "victoriametrics" as name reference)
- Alert rule structure follows Grafana 11 unified alerting format
- Thresholds: 2s for latency P95, 0.1 req/s for errors (configurable later)
  </action>
  <verify>
    # Verify alerts YAML is valid
    python3 -c "import yaml; yaml.safe_load(open('monitoring/grafana/provisioning/alerting/alerts.yaml'))"

    # Check alerts reference expected metrics
    grep -E "alexandria_search_duration|http_server_requests" monitoring/grafana/provisioning/alerting/alerts.yaml
  </verify>
  <done>Alert rules created for high search latency and high error rate</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete monitoring stack with dashboard and alerts:
- VictoriaMetrics scraping app metrics
- Grafana with RAG dashboard (latency, throughput, logs)
- Loki receiving container logs via Alloy
- Alerts configured for latency and error thresholds
  </what-built>
  <how-to-verify>
1. Start the monitoring stack:
   ```bash
   docker compose --profile eval up -d
   ```

2. Wait for all services to be healthy (about 2-3 minutes):
   ```bash
   docker compose --profile eval ps
   ```
   All 4 services (victoriametrics, loki, alloy, grafana) should show "healthy"

3. Open Grafana at http://localhost:3000
   - Login: admin / admin (or access as anonymous viewer)

4. Check Datasources (Configuration -> Data sources):
   - VictoriaMetrics should show "Data source is working"
   - Loki should show "Data source is working"

5. Check Dashboard (Dashboards -> Browse -> Alexandria folder):
   - Open "Alexandria RAG" dashboard
   - Verify panels show data (or "No data" if app not running - that's OK)
   - Panels should include: Search Rate, Latency P50/P95/P99, Ingestion Count, Logs

6. Check Alerts (Alerting -> Alert rules):
   - "Alexandria Alerts" folder should contain:
     - "High Search Latency" rule
     - "High Error Rate" rule

7. If app is running, generate some traffic to see metrics:
   ```bash
   # From another terminal, trigger some searches
   curl -X POST http://localhost:8080/api/search -H "Content-Type: application/json" -d '{"query":"test"}'
   ```
   Then check dashboard updates.
  </how-to-verify>
  <resume-signal>Type "approved" if dashboard shows panels and alerts exist, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. Dashboard provider config exists:
   ```bash
   cat monitoring/grafana/provisioning/dashboards/dashboards.yaml
   ```

2. Dashboard JSON is valid:
   ```bash
   python3 -c "import json; json.load(open('monitoring/grafana/provisioning/dashboards/alexandria-rag.json'))"
   ```

3. Alerts YAML is valid:
   ```bash
   python3 -c "import yaml; yaml.safe_load(open('monitoring/grafana/provisioning/alerting/alerts.yaml'))"
   ```

4. All monitoring files in place:
   ```bash
   ls -la monitoring/grafana/provisioning/dashboards/
   ls -la monitoring/grafana/provisioning/alerting/
   ```
</verification>

<success_criteria>
- [ ] Dashboard provider YAML created
- [ ] Dashboard JSON includes search latency panels (P50, P95, P99)
- [ ] Dashboard JSON includes ingestion rate panel
- [ ] Dashboard JSON includes error rate panel
- [ ] Dashboard JSON includes Loki logs panel
- [ ] Alert rules YAML includes high latency alert (threshold: 2s)
- [ ] Alert rules YAML includes high error rate alert
- [ ] Human verifies: Grafana shows dashboard with all panels
- [ ] Human verifies: Grafana shows alert rules in Alexandria folder
</success_criteria>

<output>
After completion, create `.planning/phases/16-monitoring-stack/16-03-SUMMARY.md`
</output>
