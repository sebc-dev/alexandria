---
phase: 16-monitoring-stack
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - docker-compose.yml
  - monitoring/loki/loki-config.yaml
  - monitoring/alloy/config.alloy
  - monitoring/grafana/provisioning/datasources/datasources.yaml
autonomous: true

must_haves:
  truths:
    - "docker compose --profile eval up starts Loki"
    - "docker compose --profile eval up starts Alloy"
    - "Alloy collects Docker container logs"
    - "Logs are visible in Grafana via Loki datasource"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Loki and Alloy services with eval profile"
      contains: "grafana/loki:3.4.0"
    - path: "monitoring/loki/loki-config.yaml"
      provides: "Loki server configuration"
      contains: "http_listen_port: 3100"
    - path: "monitoring/alloy/config.alloy"
      provides: "Alloy log collection configuration"
      contains: "loki.source.docker"
    - path: "monitoring/grafana/provisioning/datasources/datasources.yaml"
      provides: "Loki datasource for Grafana"
      contains: "type: loki"
  key_links:
    - from: "Alloy config.alloy"
      to: "Loki"
      via: "loki.write endpoint"
      pattern: "url.*loki:3100"
    - from: "Alloy config.alloy"
      to: "Docker socket"
      via: "discovery.docker"
      pattern: "unix:///var/run/docker.sock"
    - from: "Grafana datasources.yaml"
      to: "Loki"
      via: "datasource url"
      pattern: "url.*loki:3100"
---

<objective>
Add Loki and Grafana Alloy services for centralized log aggregation

Purpose: Enable viewing application logs in Grafana alongside metrics
Output: Docker container logs flowing through Alloy to Loki, queryable in Grafana
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-monitoring-stack/16-RESEARCH.md
@.planning/phases/16-monitoring-stack/16-01-SUMMARY.md
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Loki service with configuration</name>
  <files>
    - docker-compose.yml
    - monitoring/loki/loki-config.yaml
  </files>
  <action>
1. Create `monitoring/loki/loki-config.yaml`:
   ```yaml
   auth_enabled: false

   server:
     http_listen_port: 3100

   common:
     path_prefix: /loki
     storage:
       filesystem:
         chunks_directory: /loki/chunks
         rules_directory: /loki/rules
     replication_factor: 1
     ring:
       kvstore:
         store: inmemory

   schema_config:
     configs:
       - from: 2020-10-24
         store: tsdb
         object_store: filesystem
         schema: v13
         index:
           prefix: index_
           period: 24h

   limits_config:
     ingestion_burst_size_mb: 16

   query_scheduler:
     max_outstanding_requests_per_tenant: 2048
   ```

2. Add Loki service to docker-compose.yml (after victoriametrics, before grafana):
   ```yaml
   loki:
     image: grafana/loki:3.4.0
     container_name: alexandria-loki
     profiles: ["eval"]
     networks:
       - internal
     volumes:
       - loki-data:/loki
       - ./monitoring/loki/loki-config.yaml:/etc/loki/config.yaml:ro
     command: -config.file=/etc/loki/config.yaml
     healthcheck:
       test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:3100/ready || exit 1"]
       interval: 10s
       timeout: 5s
       retries: 5
       start_period: 30s
     restart: unless-stopped
   ```

3. Add `loki-data` to volumes section.

Note: Use Loki 3.4.0 (not 3.6.0+) for reliable health checks. The health check uses wget with CMD-SHELL.
  </action>
  <verify>
    docker compose --profile eval config | grep -A 25 "loki:"
    # Should show loki service with health check and config mount
  </verify>
  <done>Loki service defined with eval profile, config mounted, health check configured</done>
</task>

<task type="auto">
  <name>Task 2: Add Alloy service and update Grafana dependencies</name>
  <files>
    - docker-compose.yml
    - monitoring/alloy/config.alloy
    - monitoring/grafana/provisioning/datasources/datasources.yaml
  </files>
  <action>
1. Create `monitoring/alloy/config.alloy`:
   ```hcl
   // Discover Docker containers
   discovery.docker "containers" {
     host = "unix:///var/run/docker.sock"
   }

   // Relabel to add container name and service labels
   discovery.relabel "docker_logs" {
     targets = discovery.docker.containers.targets

     rule {
       source_labels = ["__meta_docker_container_name"]
       regex         = "/(.*)"
       target_label  = "container"
     }

     rule {
       source_labels = ["__meta_docker_container_label_com_docker_compose_service"]
       target_label  = "service"
     }
   }

   // Collect logs from Docker containers
   loki.source.docker "default" {
     host       = "unix:///var/run/docker.sock"
     targets    = discovery.docker.containers.targets
     labels     = {"source" = "docker"}
     relabel_rules = discovery.relabel.docker_logs.rules
     forward_to = [loki.write.local.receiver]
   }

   // Send logs to Loki
   loki.write "local" {
     endpoint {
       url = "http://loki:3100/loki/api/v1/push"
     }
   }
   ```

2. Add Alloy service to docker-compose.yml (after loki):
   ```yaml
   alloy:
     image: grafana/alloy:1.8.0
     container_name: alexandria-alloy
     profiles: ["eval"]
     networks:
       - internal
     volumes:
       - /var/run/docker.sock:/var/run/docker.sock:ro
       - ./monitoring/alloy/config.alloy:/etc/alloy/config.alloy:ro
     command:
       - run
       - /etc/alloy/config.alloy
       - --server.http.listen-addr=0.0.0.0:12345
     depends_on:
       loki:
         condition: service_healthy
     healthcheck:
       test: ["CMD", "wget", "-q", "--spider", "http://localhost:12345/-/ready"]
       interval: 30s
       timeout: 10s
       retries: 3
       start_period: 120s
     restart: unless-stopped
   ```

3. Update Grafana service to also depend on Loki:
   ```yaml
   grafana:
     # ... existing config ...
     depends_on:
       victoriametrics:
         condition: service_healthy
       loki:
         condition: service_healthy
   ```

4. Update `monitoring/grafana/provisioning/datasources/datasources.yaml` to add Loki:
   ```yaml
   apiVersion: 1
   datasources:
     - name: VictoriaMetrics
       type: prometheus
       access: proxy
       url: http://victoriametrics:8428
       isDefault: true
       editable: false
     - name: Loki
       type: loki
       access: proxy
       url: http://loki:3100
       editable: false
   ```

Note: Alloy needs Docker socket mount for log collection. Use generous start_period (120s) for health check.
  </action>
  <verify>
    docker compose --profile eval config | grep -A 30 "alloy:"
    # Should show alloy service with Docker socket mount and loki dependency

    cat monitoring/grafana/provisioning/datasources/datasources.yaml
    # Should show both VictoriaMetrics and Loki datasources
  </verify>
  <done>Alloy service defined, Grafana depends on both VictoriaMetrics and Loki, Loki datasource provisioned</done>
</task>

</tasks>

<verification>
1. Validate docker-compose config:
   ```bash
   docker compose --profile eval config
   ```

2. Verify all four eval services exist:
   ```bash
   docker compose --profile eval config | grep -E "victoriametrics:|loki:|alloy:|grafana:" | wc -l
   # Should output 4
   ```

3. Verify Alloy has Docker socket mount:
   ```bash
   docker compose --profile eval config | grep -A 15 "alloy:" | grep "docker.sock"
   ```

4. Verify Grafana depends on both VictoriaMetrics and Loki:
   ```bash
   docker compose --profile eval config | grep -A 10 "grafana:" | grep -A 5 "depends_on:"
   ```

5. Verify Loki datasource in Grafana:
   ```bash
   cat monitoring/grafana/provisioning/datasources/datasources.yaml | grep -A 3 "type: loki"
   ```

6. Verify Alloy config targets Loki:
   ```bash
   cat monitoring/alloy/config.alloy | grep "loki:3100"
   ```
</verification>

<success_criteria>
- [ ] Loki service defined with eval profile and health check
- [ ] Alloy service defined with Docker socket mount
- [ ] Alloy depends on Loki with service_healthy condition
- [ ] Grafana depends on both VictoriaMetrics and Loki
- [ ] Alloy config discovers Docker containers and forwards to Loki
- [ ] Loki datasource provisioned in Grafana
- [ ] `docker compose --profile eval config` validates without errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-monitoring-stack/16-02-SUMMARY.md`
</output>
