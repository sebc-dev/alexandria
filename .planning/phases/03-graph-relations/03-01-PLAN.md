---
phase: 03-graph-relations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/fr/kalifazzia/alexandria/core/port/GraphRepository.java
  - src/main/java/fr/kalifazzia/alexandria/infra/persistence/AgeGraphRepository.java
  - src/main/java/fr/kalifazzia/alexandria/core/ingestion/IngestionService.java
  - src/test/java/fr/kalifazzia/alexandria/core/ingestion/IngestionServiceTest.java
autonomous: true

must_haves:
  truths:
    - "Document vertices are created in AGE graph when documents are ingested"
    - "Chunk vertices are created in AGE graph when chunks are saved"
    - "HAS_CHILD edges link parent chunks to their child chunks"
    - "Vertices and edges are deleted when a document is re-indexed"
  artifacts:
    - path: "src/main/java/fr/kalifazzia/alexandria/core/port/GraphRepository.java"
      provides: "Port interface for graph operations"
      exports: ["createDocumentVertex", "createChunkVertex", "createParentChildEdge", "deleteDocumentGraph"]
    - path: "src/main/java/fr/kalifazzia/alexandria/infra/persistence/AgeGraphRepository.java"
      provides: "AGE implementation via JDBC with Cypher queries"
      min_lines: 80
    - path: "src/main/java/fr/kalifazzia/alexandria/core/ingestion/IngestionService.java"
      provides: "Orchestration with graph vertex and edge creation"
      contains: "graphRepository"
  key_links:
    - from: "IngestionService.java"
      to: "GraphRepository"
      via: "constructor injection"
      pattern: "GraphRepository graphRepository"
    - from: "AgeGraphRepository.java"
      to: "cypher()"
      via: "JdbcTemplate execute"
      pattern: "cypher\\('alexandria'"
---

<objective>
Create the GraphRepository port interface and AGE adapter, then integrate parent-child edge creation into the ingestion pipeline.

Purpose: Enable graph-based relationships between chunks to support Phase 5 graph traversal search.
Output: GraphRepository port/adapter, IngestionService extended to create Document and Chunk vertices plus HAS_CHILD edges.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-graph-relations/03-RESEARCH.md

# Existing code to extend
@src/main/java/fr/kalifazzia/alexandria/core/ingestion/IngestionService.java
@src/main/java/fr/kalifazzia/alexandria/core/port/DocumentRepository.java
@src/main/java/fr/kalifazzia/alexandria/core/port/ChunkRepository.java
@src/main/java/fr/kalifazzia/alexandria/core/model/ChunkType.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GraphRepository port and AgeGraphRepository adapter</name>
  <files>
    src/main/java/fr/kalifazzia/alexandria/core/port/GraphRepository.java
    src/main/java/fr/kalifazzia/alexandria/infra/persistence/AgeGraphRepository.java
  </files>
  <action>
    Create GraphRepository port interface in core/port/ with methods:
    - createDocumentVertex(UUID documentId, String path)
    - createChunkVertex(UUID chunkId, ChunkType type, UUID documentId)
    - createParentChildEdge(UUID parentId, UUID childId)
    - deleteDocumentGraph(UUID documentId) - deletes document vertex and all related chunks/edges
    - deleteChunksByDocumentId(UUID documentId) - deletes chunk vertices for a document

    Create AgeGraphRepository in infra/persistence/:
    - Use @Repository annotation
    - Inject JdbcTemplate via constructor
    - Graph name constant: "alexandria"
    - Implement Cypher queries wrapped in cypher() function
    - Use AS (result agtype) clause for all queries
    - Implement escapeCypher() for string escaping (single quotes -> '', backslashes -> \\)
    - Use DETACH DELETE for vertex deletion (cascades to edges)

    Follow patterns from 03-RESEARCH.md Pattern 1, 2, 3.
    Do NOT add Gson dependency - only needed for query results (not used in this plan).
  </action>
  <verify>
    mvn compile -q succeeds
    GraphRepository.java exists with all 5 methods
    AgeGraphRepository.java exists with @Repository and JdbcTemplate injection
  </verify>
  <done>
    GraphRepository port defines graph operations contract
    AgeGraphRepository implements Cypher queries via JDBC
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate GraphRepository into IngestionService</name>
  <files>
    src/main/java/fr/kalifazzia/alexandria/core/ingestion/IngestionService.java
    src/test/java/fr/kalifazzia/alexandria/core/ingestion/IngestionServiceTest.java
  </files>
  <action>
    Extend IngestionService to use GraphRepository:

    1. Add GraphRepository as constructor parameter (after ChunkRepository)
    2. In ingestFile(), after document save:
       - Call graphRepository.createDocumentVertex(document.id(), document.path())
    3. In the chunk processing loop, after saving parent chunk:
       - Call graphRepository.createChunkVertex(parentId, ChunkType.PARENT, document.id())
    4. After saving each child chunk:
       - Call graphRepository.createChunkVertex(childId, ChunkType.CHILD, document.id())
       - Call graphRepository.createParentChildEdge(parentId, childId)
    5. In the delete section (when re-indexing), BEFORE deleting chunks from PostgreSQL:
       - Call graphRepository.deleteChunksByDocumentId(existingId)
       - Call graphRepository.deleteDocumentGraph(existingId)

    Update IngestionServiceTest:
    - Add GraphRepository mock to test setup
    - Verify createDocumentVertex called on successful ingest
    - Verify createChunkVertex called for parent and children
    - Verify createParentChildEdge called with correct IDs
    - Verify deleteChunksByDocumentId and deleteDocumentGraph called on re-index

    The GraphRepository is optional in tests - mock it to verify calls without actual AGE.
  </action>
  <verify>
    mvn test -Dtest=IngestionServiceTest -q passes
    All existing tests still pass
    New tests verify graph operations are called
  </verify>
  <done>
    IngestionService creates graph vertices and HAS_CHILD edges during ingestion
    Graph data is cleaned up during document re-indexing
    Unit tests verify graph integration without requiring Docker
  </done>
</task>

</tasks>

<verification>
1. mvn compile -q succeeds with no errors
2. mvn test -q passes all unit tests
3. GraphRepository interface has 5 methods (create/delete operations)
4. AgeGraphRepository uses JdbcTemplate and cypher() function pattern
5. IngestionService constructor accepts GraphRepository parameter
6. IngestionServiceTest verifies graph operations are invoked
</verification>

<success_criteria>
- GraphRepository port interface exists with CRUD operations for graph entities
- AgeGraphRepository adapter implements Cypher queries via JDBC
- IngestionService creates Document and Chunk vertices during ingestion
- IngestionService creates HAS_CHILD edges linking parent chunks to children
- Graph data is deleted when documents are re-indexed (upsert pattern)
- All unit tests pass (existing + new graph verification tests)
</success_criteria>

<output>
After completion, create `.planning/phases/03-graph-relations/03-01-SUMMARY.md`
</output>
