---
phase: 02-ingestion-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - src/main/java/fr/kalifazzia/alexandria/core/model/Document.java
  - src/main/java/fr/kalifazzia/alexandria/core/model/Chunk.java
  - src/main/java/fr/kalifazzia/alexandria/core/model/ChunkType.java
  - src/main/java/fr/kalifazzia/alexandria/core/ingestion/DocumentMetadata.java
  - src/main/java/fr/kalifazzia/alexandria/core/ingestion/ParsedDocument.java
  - src/main/java/fr/kalifazzia/alexandria/core/ingestion/MarkdownParser.java
  - src/main/java/fr/kalifazzia/alexandria/core/port/DocumentRepository.java
  - src/main/java/fr/kalifazzia/alexandria/infra/persistence/JdbcDocumentRepository.java
  - src/test/java/fr/kalifazzia/alexandria/core/ingestion/MarkdownParserTest.java
autonomous: true

must_haves:
  truths:
    - "Markdown files with YAML frontmatter are parsed correctly"
    - "Title, tags, and category are extracted from frontmatter"
    - "Content without frontmatter is available for chunking"
    - "Documents can be saved to database with metadata"
  artifacts:
    - path: "src/main/java/fr/kalifazzia/alexandria/core/model/Document.java"
      provides: "Domain entity for indexed documents"
      contains: "record Document"
    - path: "src/main/java/fr/kalifazzia/alexandria/core/ingestion/MarkdownParser.java"
      provides: "Markdown parsing with frontmatter extraction"
      exports: ["parse"]
    - path: "src/main/java/fr/kalifazzia/alexandria/core/port/DocumentRepository.java"
      provides: "Port interface for document persistence"
      exports: ["save", "findByPath", "delete"]
    - path: "src/main/java/fr/kalifazzia/alexandria/infra/persistence/JdbcDocumentRepository.java"
      provides: "JDBC implementation of DocumentRepository"
      contains: "implements DocumentRepository"
  key_links:
    - from: "MarkdownParser"
      to: "ParsedDocument"
      via: "parse() returns ParsedDocument record"
      pattern: "ParsedDocument parse"
    - from: "JdbcDocumentRepository"
      to: "DocumentRepository"
      via: "implements port interface"
      pattern: "implements DocumentRepository"
---

<objective>
Create domain models, markdown parser with YAML frontmatter extraction, and document repository for Phase 2 ingestion pipeline.

Purpose: Foundation layer providing core entities and parsing capability needed by subsequent chunking and embedding plans.
Output: Document/Chunk models, MarkdownParser class, DocumentRepository port and JDBC adapter.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ingestion-core/02-RESEARCH.md
@src/main/resources/db/changelog/changes/002-schema.sql
@pom.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CommonMark dependencies and create domain models</name>
  <files>
    pom.xml
    src/main/java/fr/kalifazzia/alexandria/core/model/Document.java
    src/main/java/fr/kalifazzia/alexandria/core/model/Chunk.java
    src/main/java/fr/kalifazzia/alexandria/core/model/ChunkType.java
    src/main/java/fr/kalifazzia/alexandria/core/ingestion/DocumentMetadata.java
    src/main/java/fr/kalifazzia/alexandria/core/ingestion/ParsedDocument.java
  </files>
  <action>
    1. Add CommonMark dependencies to pom.xml:
       ```xml
       <dependency>
           <groupId>org.commonmark</groupId>
           <artifactId>commonmark</artifactId>
           <version>0.22.0</version>
       </dependency>
       <dependency>
           <groupId>org.commonmark</groupId>
           <artifactId>commonmark-ext-yaml-front-matter</artifactId>
           <version>0.22.0</version>
       </dependency>
       ```

    2. Create Document record in core/model/:
       - Fields matching database schema: id (UUID), path (String), title (String), category (String), tags (List<String>), contentHash (String), frontmatter (Map<String, Object>), createdAt (Instant), updatedAt (Instant)
       - Use Java 21 record
       - Include factory method for new documents (without id/timestamps)

    3. Create ChunkType enum: PARENT, CHILD

    4. Create Chunk record in core/model/:
       - Fields: id (UUID), documentId (UUID), parentChunkId (UUID nullable), type (ChunkType), content (String), position (int), createdAt (Instant)
       - Use Java 21 record

    5. Create DocumentMetadata record in core/ingestion/:
       - Fields: title (String), category (String), tags (List<String>), rawFrontmatter (Map<String, List<String>>)
       - This is the structured output from frontmatter parsing

    6. Create ParsedDocument record in core/ingestion/:
       - Fields: metadata (DocumentMetadata), content (String - markdown without frontmatter)
  </action>
  <verify>
    mvn compile
    Check that all model classes compile without errors.
  </verify>
  <done>
    - CommonMark 0.22.0 dependencies added to pom.xml
    - Document, Chunk, ChunkType records exist in core/model/
    - DocumentMetadata, ParsedDocument records exist in core/ingestion/
    - Project compiles successfully
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MarkdownParser with frontmatter extraction</name>
  <files>
    src/main/java/fr/kalifazzia/alexandria/core/ingestion/MarkdownParser.java
    src/test/java/fr/kalifazzia/alexandria/core/ingestion/MarkdownParserTest.java
  </files>
  <action>
    1. Create MarkdownParser class in core/ingestion/:
       - Use CommonMark Parser with YamlFrontMatterExtension
       - Method: ParsedDocument parse(String content)
       - Extract frontmatter using YamlFrontMatterVisitor
       - Map frontmatter keys to DocumentMetadata fields:
         - "title" -> title (first value or null)
         - "category" -> category (first value or null)
         - "tags" -> tags (list or empty list)
       - Strip frontmatter from content using extractContentAfterFrontmatter() helper
       - Frontmatter block is delimited by --- at start and --- after YAML

    2. Implementation details from RESEARCH.md:
       ```java
       private final Parser parser;

       public MarkdownParser() {
           this.parser = Parser.builder()
               .extensions(List.of(YamlFrontMatterExtension.create()))
               .build();
       }

       public ParsedDocument parse(String content) {
           Node document = parser.parse(content);
           YamlFrontMatterVisitor visitor = new YamlFrontMatterVisitor();
           document.accept(visitor);
           Map<String, List<String>> frontmatter = visitor.getData();
           // ... extract fields and clean content
       }
       ```

    3. Create unit tests in MarkdownParserTest:
       - Test parsing file with full frontmatter (title, category, tags)
       - Test parsing file with partial frontmatter (only title)
       - Test parsing file with no frontmatter
       - Test that content after frontmatter is correctly extracted
       - Test tags as YAML list format

    4. Handle edge cases:
       - Empty content -> return empty ParsedDocument
       - Missing frontmatter fields -> null/empty defaults
       - Malformed frontmatter -> treat as no frontmatter
  </action>
  <verify>
    mvn test -Dtest=MarkdownParserTest
    All unit tests pass.
  </verify>
  <done>
    - MarkdownParser class parses markdown with YAML frontmatter
    - Title, category, tags extracted from frontmatter
    - Content without frontmatter block returned
    - Unit tests cover normal and edge cases
  </done>
</task>

<task type="auto">
  <name>Task 3: Create DocumentRepository port and JDBC adapter</name>
  <files>
    src/main/java/fr/kalifazzia/alexandria/core/port/DocumentRepository.java
    src/main/java/fr/kalifazzia/alexandria/infra/persistence/JdbcDocumentRepository.java
  </files>
  <action>
    1. Create DocumentRepository interface in core/port/:
       - Method: Document save(Document document) - insert new document, returns with generated id
       - Method: Optional<Document> findByPath(String path) - find by unique path
       - Method: void delete(UUID id) - delete document by id
       - Method: void deleteByPath(String path) - delete by path (for upsert pattern)
       - This is a PORT interface - no implementation details

    2. Create JdbcDocumentRepository in infra/persistence/:
       - Inject JdbcTemplate via constructor
       - Annotate with @Repository and @Component
       - Implement save():
         - Generate UUID for new document
         - Insert into documents table: id, path, title, category, tags (TEXT[]), content_hash, frontmatter (JSONB)
         - Use jdbcTemplate.update() with PreparedStatement
         - For tags array: use connection.createArrayOf("text", tags.toArray())
         - For frontmatter JSONB: use PGobject with type "jsonb" and JSON string value
         - Return Document with generated id and current timestamps

    3. Implement findByPath():
       - Query: SELECT * FROM documents WHERE path = ?
       - Map ResultSet to Document record
       - Return Optional.empty() if not found

    4. Implement delete() and deleteByPath():
       - Simple DELETE statements

    5. Add ObjectMapper for JSONB serialization:
       - Use Jackson ObjectMapper (already in Spring Boot)
       - Serialize frontmatter Map to JSON string for storage
       - Deserialize JSONB back to Map on read
  </action>
  <verify>
    mvn compile
    Check that JdbcDocumentRepository compiles and implements all port methods.
  </verify>
  <done>
    - DocumentRepository port interface defined in core/port/
    - JdbcDocumentRepository implements all methods with JDBC
    - Tags stored as TEXT[] array
    - Frontmatter stored as JSONB
    - findByPath returns Optional for upsert detection
  </done>
</task>

</tasks>

<verification>
- `mvn compile` succeeds
- `mvn test -Dtest=MarkdownParserTest` passes all tests
- Document and Chunk records match database schema fields
- MarkdownParser correctly extracts frontmatter from sample markdown
- DocumentRepository port follows dependency inversion principle
</verification>

<success_criteria>
- CommonMark dependencies in pom.xml
- 5 model/record classes created (Document, Chunk, ChunkType, DocumentMetadata, ParsedDocument)
- MarkdownParser parses frontmatter and returns clean content
- DocumentRepository port and JdbcDocumentRepository adapter implemented
- All code compiles, unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingestion-core/02-01-SUMMARY.md`
</output>
