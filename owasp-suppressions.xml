<?xml version="1.0" encoding="UTF-8"?>
<suppressions xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.3.xsd">

    <!-- ===================================================================
         Spring AI MCP Server — False positive VMware Workstation CVEs
         The dependency-check scanner misidentifies Spring AI libraries
         (spring-ai-autoconfigure-mcp-server, spring-ai-starter-mcp-server-webmvc)
         as VMware Workstation due to CPE name collision with "workstation"
         in the artifact metadata. These CVEs (2007-2009 era) relate to
         VMware desktop software ActiveX controls, DHCP services, etc.
         and have zero relevance to a Java Spring Boot library.
         =================================================================== -->
    <suppress>
        <notes>False positive: Spring AI MCP library misidentified as VMware Workstation</notes>
        <gav regex="true">org\.springframework\.ai:spring-ai-.*</gav>
        <cpe regex="true">cpe:/a:vmware:workstation.*</cpe>
    </suppress>
    <suppress>
        <notes>False positive: Spring AI MCP library misidentified as VMware Server</notes>
        <gav regex="true">org\.springframework\.ai:spring-ai-.*</gav>
        <cpe regex="true">cpe:/a:vmware:server.*</cpe>
    </suppress>
    <suppress>
        <notes>False positive: Spring AI MCP library misidentified as VMware Player</notes>
        <gav regex="true">org\.springframework\.ai:spring-ai-.*</gav>
        <cpe regex="true">cpe:/a:vmware:player.*</cpe>
    </suppress>
    <suppress>
        <notes>False positive: Spring AI MCP library misidentified as VMware ACE</notes>
        <gav regex="true">org\.springframework\.ai:spring-ai-.*</gav>
        <cpe regex="true">cpe:/a:vmware:ace.*</cpe>
    </suppress>

    <!-- ===================================================================
         CVE-2023-2976: Guava FileBackedOutputStream temp directory vulnerability
         Shaded inside dataflow-errorprone (Error Prone compile-time dependency).
         This is a compile-time-only tool, never runs in production. The vulnerable
         FileBackedOutputStream class is not used by Error Prone's analysis.
         =================================================================== -->
    <suppress>
        <notes>CVE-2023-2976: Guava FileBackedOutputStream temp directory vulnerability.
        Only present in shaded Guava 30.1.1-jre inside dataflow-errorprone, which is a
        compile-time-only Error Prone dependency. Never deployed to production.
        FileBackedOutputStream is not used by Error Prone analysis.</notes>
        <packageUrl regex="true">pkg:maven/com\.google\.guava/guava@30\..*</packageUrl>
        <cve>CVE-2023-2976</cve>
    </suppress>

    <!-- ===================================================================
         CVE-2024-7776: ONNX Python download_model function vulnerability
         The CVE applies to the Python onnx package's download_model function.
         langchain4j-onnx-scoring is a Java library using ONNX Runtime (not
         the Python onnx package) and does not contain the vulnerable code path.
         =================================================================== -->
    <suppress>
        <notes>CVE-2024-7776: Python onnx download_model vulnerability, not present in Java ONNX Runtime</notes>
        <gav regex="true">dev\.langchain4j:langchain4j-onnx-scoring:.*</gav>
        <cve>CVE-2024-7776</cve>
    </suppress>

</suppressions>
